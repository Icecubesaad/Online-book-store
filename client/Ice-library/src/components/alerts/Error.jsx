import React, { useContext } from "react";

const Error = () => {
  return (
    <div class="alert alert-warning" role="alert">
      Book is already in your account!
    </div>
  );
};

export default Error;
